// import './style.css'

class Todo {
  constructor(id, text) {
      this.id = id;
      this.text = text;
  }

  // get id() {
  //   return this.id
  // }
}

class TodoManager {
  constructor(todoId) {
    this.todoId = todoId;
  }
}